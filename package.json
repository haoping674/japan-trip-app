{
  "name": "japan-trip-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check sw.js && node --check api/state.js",
    "vercel-dev": "vercel dev --listen 4173"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  },
  "devDependencies": {
    "vercel": "^48.6.6"
  }
}
